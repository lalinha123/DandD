<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= party.name %>!</title>
</head>
<body>
    <% if (user) { %>
        <h1><%= party.name %>!!</h1>
        <h3>The most incredible party!!!!!!!!!!!!</h3>
        <h3><%= party.maxparticipants %></h3>

        <%  if (userrole === 'master' || userrole === 'manager') { %>
                <a href="/parties/<%= party.id %>/edit"><button>Edit party</button></a>
                <br>
                <form action="/parties/<%= party.id %>/delete" method="GET">
                    <input type="submit" value="Delete party">
                </form>
        <% } %>
    <% } else { %>
        <hr>
        
        <h2>uh oh!</h2>
        <p>It seems you're not logged in yet!</p>
        <p>click <a href="/login">here</a> to log in or <a href="/signup">here</a> to sign up!!!</p>
    <% } %>
</body>
</html>